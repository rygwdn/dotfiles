" vim: fdm=marker
" vim: foldlevel=0

map < :tabmove -1<CR>
map > :tabmove +1<CR>
map J :js gBrowser.tabContainer.advanceSelectedTab(1, true)<CR>
map K :js gBrowser.tabContainer.advanceSelectedTab(-1, true)<CR>
map D :emenu Delicious.Save a new Bookmark...<CR>
js config.guioptions['D'] = ['Delicious Toolbar',['ybToolbar']];
set go+=Trm

command lastpasslogin javascript LP.lpOpenLogin()
command lp lastpasslogin

command google open google
command g google

javascript <<EOM
if (liberator.has("MacUnix")) {
    // for Mac
} else if (liberator.has("Win32")) {
    // for Windows
} else {
    // other OS (linux)
}
EOM
