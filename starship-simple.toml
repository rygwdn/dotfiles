"$schema" = 'https://starship.rs/config-schema.json'

add_newline = false

# Simple format - left prompt only with path and all enabled modules
format = "${custom.shortpath}$git_branch$character"

command_timeout = 5000

[custom.shortpath]
command = "jumpr shortpath --section full $PWD"
when = "command -v jumpr"
format = "[$output]($style) "
style = "cyan"
description = "Shortened path from jumpr"

[git_branch]
format = '[$symbol$branch(:$remote_branch)]($style) '
